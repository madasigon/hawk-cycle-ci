name: Infrastructure Lint

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - '.github/workflows/infra-lint.yml'
      - 'infra/**/*.py'
      - 'infra/**/*.yaml'
      - 'scripts/ci/**/*.py'
      - 'scripts/dev/deploy-teardown-test.py'
      - 'scripts/dev/test_deploy_teardown.py'
      - 'pyproject.toml'
      - 'uv.lock'
      - 'hawk/.dockerignore'
      - 'middleman/Dockerfile'
      - 'middleman/.dockerignore'
      - 'relay/Dockerfile'
      - 'relay/.dockerignore'
      - 'Pulumi*.yaml'
      - 'datadog/modules/**'
      - 'datadog/package*.json'

permissions:
  contents: read

jobs:
  pulumi-lint-and-test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          version: "0.12.3"
          enable-cache: true
          save-cache: ${{ github.ref == 'refs/heads/main' }}
      - name: Install Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5.0.1
        with:
          version: "v3.17.2"
      - name: Install dependencies
        run: uv sync --locked --directory infra
      - name: Syntax check
        run: uv run --directory infra python -m py_compile __main__.py
      - name: Run infra tests
        run: uv run --directory infra python -m pytest tests/ -v

      # The scripts/ci helpers are standalone uv scripts outside any project's testpaths,
      # so run them here rather than leaving their tests unexecuted. The deploy/teardown
      # harness tests (scripts/dev) ride along: same standalone shape; they
      # need pyyaml to load the generated stack config.
      - name: Run CI script tests
        run: uv run --isolated --no-project --with pytest --with boto3 --with pyyaml python -m pytest scripts/ci scripts/dev/test_deploy_teardown.py -v

  datadog-javascript-tests:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'
      - name: Install dependencies
        run: npm ci
        working-directory: datadog
      - name: Run Datadog workflow tests
        run: npx jest --ci
        working-directory: datadog

  python-tests:
    runs-on: ubuntu-24.04
    needs: [pulumi-lint-and-test, datadog-javascript-tests]
    steps:
      - run: echo "All tests passed!"
